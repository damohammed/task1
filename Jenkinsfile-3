pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
        docker ps -a
        node --version
        git --version
        curl --version
        '''
      }
    }
  }
}
