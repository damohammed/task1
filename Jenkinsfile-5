pipeline {
   docker { 
      agent { 'nice_tharp' 
    args '-u root:sudo' } 
  }
  stages { 
    stage('ps')  { 
      steps {
        sh ''' 
        docker ps -a
        ''' } 
           } 
         }
       }
